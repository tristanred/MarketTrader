# Shell scripts are consumed by Linux. With core.autocrlf=true (the usual
# Windows setting) they would be checked out with CRLF, and a shell script with
# CRLF fails as `$'\r': command not found`, which gives no hint about the real
# cause. Pin them to LF everywhere.
*.sh text eol=lf
